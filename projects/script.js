document.addEventListener('DOMContentLoaded', (event) => {
    console.log('DOM fully loaded and parsed');

    // Your code here
    function initialize() {
        // Initialization code
    }

    initialize();
});